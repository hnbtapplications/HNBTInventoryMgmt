import React,{useEffect,useState}from"react";
import{RefreshCw,ShieldCheck,UserCheck,UserX,KeyRound,X}from"lucide-react";
import{fetchEmployeeProfiles,updateEmployeeProfile}from"./supabase";

const permissions=[
 ["can_manage_products","Products"],["can_stock_movement","Stock Movement"],["can_view_reports","Reports"],["can_manage_masters","Masters"],["can_view_audit_logs","Audit Logs"],["can_manage_users","User Management"]
];
export default function UserManagement({currentProfile}){
 const[users,setUsers]=useState([]),[busy,setBusy]=useState(true),[error,setError]=useState(""),[notice,setNotice]=useState("");
 const[resetUser,setResetUser]=useState(null),[newPassword,setNewPassword]=useState(""),[resetBusy,setResetBusy]=useState(false);
 const isAdmin=currentProfile?.role==="admin"&&currentProfile?.can_manage_users;
 async function load(){setBusy(true);setError("");try{setUsers(await fetchEmployeeProfiles())}catch(e){setError(e?.message||"Unable to load employees")}finally{setBusy(false)}}
 useEffect(()=>{if(isAdmin)load()},[isAdmin]);
 async function patch(userId,changes){try{await updateEmployeeProfile(userId,changes);setUsers(u=>u.map(x=>x.user_id===userId?{...x,...changes}:x))}catch(e){setError(e?.message||"Unable to update employee")}}
 async function resetPassword(e){e.preventDefault();if(!resetUser)return;setResetBusy(true);setError("");setNotice("");try{const r=await fetch("/api/admin-reset-password",{method:"POST",headers:{"Content-Type":"application/json"},credentials:"same-origin",body:JSON.stringify({user_id:resetUser.user_id,new_password:newPassword})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||"Unable to reset password");setNotice(`${resetUser.full_name}: temporary password set successfully. Employee must change it after login.`);setResetUser(null);setNewPassword("")}catch(e2){setError(e2?.message||"Unable to reset password")}finally{setResetBusy(false)}}
 if(!isAdmin)return <div className="empty"><ShieldCheck size={28}/><h3>Administrator access required</h3><p>You do not have permission to manage employee accounts.</p></div>;
 return <section className="user-management"><div className="section-head"><div><h2>Employee User Management</h2><p>Control branch access, roles, permissions and password recovery.</p></div><button className="secondary" onClick={load} disabled={busy}><RefreshCw size={16}/>{busy?"Loading...":"Refresh"}</button></div>{error&&<div className="login-error">{error}</div>}{notice&&<div className="cloud-status-banner success">✅ {notice}</div>}<div className="employee-grid">{users.map(u=><article className="employee-card" key={u.user_id}><div className="employee-title"><div><strong>{u.full_name}</strong><small>{u.role==="admin"?"Administrator":"Employee"}</small></div><span className={u.active?"status active":"status disabled"}>{u.active?<UserCheck size={15}/>:<UserX size={15}/>} {u.active?"Active":"Disabled"}</span></div><div className="employee-fields"><label>Role<select value={u.role} onChange={e=>patch(u.user_id,{role:e.target.value})}><option value="employee">Employee</option><option value="admin">Administrator</option></select></label><label>Branch<select value={u.branch} onChange={e=>patch(u.user_id,{branch:e.target.value})}><option>Bangalore</option><option>Hosur</option><option>Both</option></select></label></div><div className="permission-grid">{permissions.map(([key,label])=><label key={key}><input type="checkbox" checked={Boolean(u[key])} onChange={e=>patch(u.user_id,{[key]:e.target.checked})}/><span>{label}</span></label>)}</div><div style={{display:"flex",gap:8,flexWrap:"wrap"}}><button className="secondary" onClick={()=>{setResetUser(u);setNewPassword("");setError("");setNotice("")}}><KeyRound size={15}/> Reset Password</button><button className={u.active?"danger-outline":"secondary"} onClick={()=>patch(u.user_id,{active:!u.active})}>{u.active?"Disable User":"Enable User"}</button></div></article>)}</div>{!busy&&!users.length&&<div className="empty">No employee profiles found.</div>}{resetUser&&<div className="overlay"><div className="modal" style={{maxWidth:480}}><div className="modal-head"><h2>Reset Password — {resetUser.full_name}</h2><button type="button" onClick={()=>{setResetUser(null);setNewPassword("")}}><X/></button></div><form className="form" onSubmit={resetPassword}><div className="cloud-setup-box"><h4><KeyRound size={16}/> Temporary Password</h4><p>The existing password cannot be viewed. Set a temporary password below. The employee will be required to create a new private password after signing in.</p></div><label className="field"><span>New Temporary Password</span><input type="password" minLength={8} autoComplete="new-password" required value={newPassword} onChange={e=>setNewPassword(e.target.value)} placeholder="Minimum 8 characters"/></label><div className="modal-actions"><button type="button" className="ghost" onClick={()=>{setResetUser(null);setNewPassword("")}}>Cancel</button><button className="primary" disabled={resetBusy}><KeyRound size={16}/>{resetBusy?"Resetting...":"Set Temporary Password"}</button></div></form></div></div>}</section>;
}

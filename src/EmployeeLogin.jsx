import React,{useState}from"react";
import{LogIn,ShieldCheck}from"lucide-react";

export default function EmployeeLogin({onAuthenticated,onUseLegacyAdmin}){
 const[username,setUsername]=useState("");const[password,setPassword]=useState("");const[busy,setBusy]=useState(false);const[error,setError]=useState("");
 async function submit(e){e.preventDefault();setBusy(true);setError("");try{const response=await fetch("/api/login",{method:"POST",headers:{"Content-Type":"application/json"},credentials:"same-origin",body:JSON.stringify({username:username.trim(),password})});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||"Unable to sign in");onAuthenticated?.(data)}catch(err){setError(err?.message||"Unable to sign in")}finally{setBusy(false)}}
 return <div className="login-screen"><div className="login-card"><div className="login-brand"><img src="/logo.png" alt="Hertz & Bytes Technologies"/><div><h1>Inventory Management</h1><p>Employee Login</p></div></div><div className="login-security"><ShieldCheck size={18}/><span>Individual employee access & activity tracking</span></div><form onSubmit={submit}><label>Username</label><input type="text" autoComplete="username" value={username} onChange={e=>setUsername(e.target.value)} placeholder="e.g. saravanan" required/><label>Password</label><input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter password" required/>{error&&<div className="login-error">{error}</div>}<button className="primary login-submit" type="submit" disabled={busy}><LogIn size={17}/>{busy?"Signing in...":"Sign In"}</button></form>{onUseLegacyAdmin&&<button type="button" className="legacy-login-link" onClick={onUseLegacyAdmin}>Administrator fallback login</button>}<p className="login-footnote">Hertz & Bytes Technologies · Bangalore & Hosur</p></div></div>;
}
